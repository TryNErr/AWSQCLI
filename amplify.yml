version: 1
backend:
  phases:
    build:
      commands:
        - echo "🔧 Amplify Gen 2 Backend Deployment"
        - cd amplify
        - npm install
        - npm run build
        - echo "✅ Backend compiled"
frontend:
  phases:
    preBuild:
      commands:
        - echo "🚀 QuizWiz Frontend Deployment"
        - pwd
        - ls -la
        - cd quizwiz-app/frontend
        - npm install
        - echo "✅ Dependencies installed"
    build:
      commands:
        - echo "🔨 Building React application..."
        - echo "REACT_APP_API_URL=$AMPLIFY_API_URL" > .env.production.local
        - echo "REACT_APP_ENVIRONMENT=production" >> .env.production.local
        - echo "GENERATE_SOURCEMAP=false" >> .env.production.local
        - npm run build
        - echo "✅ Build completed"
    postBuild:
      commands:
        - test -f build/index.html && echo "✅ Frontend ready" || exit 1
        - echo "🎉 Full-stack deployment complete"
  artifacts:
    baseDirectory: quizwiz-app/frontend/build
    files:
      - '**/*'
  cache:
    paths:
      - quizwiz-app/frontend/node_modules/**/*
      - amplify/node_modules/**/*
